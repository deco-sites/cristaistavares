import Component from "$store/components/ui/Newsletter.tsx";
import type { Props } from "$store/components/ui/Newsletter.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
